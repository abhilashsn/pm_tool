<% if @clients.empty? %>
<p> no clients found </p>
<%else%>
	<ul>
		<% for client in @clients %>
		<li style="color:magenta"><%= link_to client.name, client_path(client) %></li>
		<%end%>
	</ul>
<%end%>



	<% if can? :create, Client.new %>
	<%= link_to "Add Client", new_client_path %>
	<%end%>